```sh
  ssh-keygen
  cd .ssh/
  ls
  cat  id_ed25519.pub
    cd..
    cd.
    cd ..
    cd .
    mkdir Projects
    cd Projects/
    mkdir ICT
    cd ICT/
    git int
    ls -la
    git init
  ls -la
    echo 'Hello world' > FirstDocument.md
    ls
    git status
    git add FirstDocument.md
    ls -la
    \git status
    git commit -m 'Added first document'
    git status
    git log
    git remote add origin git@github.com:sagecoder-101/ICT.git~
   git remote add origin git@github.com:sagecoder-101/ICT.git
  git remote get-url origin
   git push -u orgin master
 

 ```